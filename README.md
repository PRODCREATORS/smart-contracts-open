# Entangle.fi Smart Contracts mirror

### Building
```sh
$ npm install
$ npm run compile
```

### Deploying
Create a dotenv with a valid bep-39 `MNEMONIC` first then run:
```sh
$ ./deploy_all.sh
```

